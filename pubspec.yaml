name: mini_project
description: A Flutter project for managing expenses.
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fluttertoast: ^8.2.1
  sqflite: ^2.2.8
  path: ^1.8.3
  provider: ^6.1.3 # Add provider for state management if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
